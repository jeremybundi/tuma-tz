import React from 'react';
import Home from './dashboard/Home'; // Adjust the path if Home.tsx is in a different folder

const Page: React.FC = () => {
  return (
    <main >
      <Home />
    </main>
  );
};

export default Page;
